version: 0.2

phases:
  install:
    commands:
      - npm install -g npm
      - npm install -f
      runtime-versions:
nodejs: 16
  build:
    commands:
      - npm run build
      - npm run test
      - npm run cdk -- synth

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'